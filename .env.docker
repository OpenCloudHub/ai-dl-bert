# =============================================================================
# .env.docker - Local Docker Compose Development Environment
# =============================================================================
#
# Purpose:
#   Environment variables for local development using the Docker Compose stack
#   from github.com/OpenCloudHub/local-compose-stack
#
# Prerequisites:
#   1. Clone and start the local-compose-stack:
#      git clone https://github.com/OpenCloudHub/local-compose-stack.git
#      cd local-compose-stack && docker compose up -d
#
#   2. Apply this configuration:
#      set -a && source .env.docker && set +a
#
# Services (from local-compose-stack):
#   - MLflow UI:       http://localhost:5000
#   - MinIO Console:   http://localhost:9001 (admin/admin123)
#   - MinIO API:       http://localhost:9000
#
# =============================================================================

# -----------------------------------------------------------------------------
# MinIO / S3 Configuration
# -----------------------------------------------------------------------------
AWS_ACCESS_KEY_ID=admin
AWS_SECRET_ACCESS_KEY=admin123
AWS_ENDPOINT_URL=http://localhost:9000

# -----------------------------------------------------------------------------
# DVC Configuration
# -----------------------------------------------------------------------------
DVC_REPO=https://github.com/OpenCloudHub/data-registry
DVC_REMOTE=docker
DVC_DATA_VERSION=emotion-v1.0.0

# -----------------------------------------------------------------------------
# MLflow Configuration
# -----------------------------------------------------------------------------
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_S3_ENDPOINT_URL=http://localhost:9000
MLFLOW_TRACKING_INSECURE_TLS=true
MLFLOW_LOGGING_LEVEL=DEBUG
MLFLOW_ENABLE_SYSTEM_METRICS_LOGGING=true
MLFLOW_EXPERIMENT_NAME=emotion
MLFLOW_REGISTERED_MODEL_NAME=ci.emotion-classifier

# -----------------------------------------------------------------------------
# Ray Configuration
# -----------------------------------------------------------------------------
RAY_TRAIN_V2_ENABLED=1
RAY_STORAGE_PATH=/tmp/ray_results

# -----------------------------------------------------------------------------
# CI/CD Tracking (DEV values for local development)
# -----------------------------------------------------------------------------
ARGO_WORKFLOW_UID=DEV
DOCKER_IMAGE_TAG=DEV

# -----------------------------------------------------------------------------
# Model Configuration
# -----------------------------------------------------------------------------
TOKENIZERS_PARALLELISM=true



